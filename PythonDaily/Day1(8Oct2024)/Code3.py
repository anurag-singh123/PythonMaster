# Write a Python program that asks the user for the length and width of a rectangle and calculates its area.

print("We are going to calculate the Area of Rectangle.")
length = float(input("Enter the length of Rectangle:"))
width = float(input("Enter the width of Rectangle:"))
area = length * width
print("Area of Rectangle is",area)